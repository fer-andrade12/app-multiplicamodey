# MultiplicaMoney Frontend (React Native - Expo)

This is a minimal Expo React Native skeleton to interact with the MultiplicaMoney API.

Install Expo CLI and run:

```bash
npm install -g expo-cli
expo init frontend --template blank
cd frontend
expo start
```

Replace `App.js` with the provided `App.js` to point to your running API.
